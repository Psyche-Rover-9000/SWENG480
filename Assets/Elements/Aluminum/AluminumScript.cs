using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AluminumScript : GenericElement
{
    // Start is called before the first frame update
    void Start()
    {
        value = 4;
        GenerateRandomVariance();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
